**【Role & Objective】**

You are an expert in 3D Point Cloud Registration (PyTorch & C++). I need you to implement the "Uncertainty-Driven Robust Registration" system by modifying the **GeoTransformer** (Python) and **TCF** (C++) codebases.

**Please strictly follow the optimized plan below.**

### Phase 1: GeoTransformer Modification (Python)

#### 1. Modify Model Architecture (Fine-Level Uncertainty)

**Target File**: `geotransformer/modules/registration/local_global_registration.py` (or where Fine Matching happens).

* **Logic Change**:
  * We need **Point-Level Uncertainty**, not Superpoint-Level.
  * Locate the **Fine Matching** stage where dense features are used to compute the final coordinates.
  * Insert a new `UncertaintyHead` (MLP: `input_dim` -> `dim//2` -> `ReLU` -> `1`) that runs in parallel with the coordinate regression.
  * **Input**: The same dense features used for fine matching (e.g., `ref_node_feats` after expansion/interpolation).
  * **Output**: A scalar $s$ (**log variance**) for every point.

#### 2. Implement Stability-Enhanced Loss Function

**Target File**: `experiments/.../loss.py`

* **Formula**:

  $$\mathcal{L}\_{unc} = \frac{1}{N} \sum \left( \frac{1}{2} e^{-s} \\| p\_{pred} - p\_{gt} \\|^2 + \frac{1}{2} s \right)$$
* **Critical Constraints (Must Implement)**:
  * **Clamping**: Before computing the loss, clamp the predicted log variance $s$ to range `[-10, 10]` to prevent gradient explosion.
  * **Weighting**: Combine this loss with the original registration loss: `Total_Loss = L_reg + 0.1 * L_unc` (start with a small weight).

#### 3. Update Output Format for C++ Compatibility

**Target File**: `experiments/.../test.py`

* **Constraint**: Do **NOT** use `.npz`. Use simplified `.txt` files for easy C++ reading.
* **Implementation**:
  * In the testing loop, after getting the final matches, save them to a `.txt` file named `{scene_id}_matches.txt`.
  * **Format**: Each line contains 8 values:

    `src_x src_y src_z tgt_x tgt_y tgt_z sigma_src sigma_tgt`
  * Where `sigma = exp(0.5 * s)`.

### Phase 2: TCF Modification (C++)

#### 1. Simple File I/O Implementation

**Target File**: `TCF-main/demo.cpp`

* **Logic**:
  * Replace the original descriptor loading logic (if any) with a simple `std::ifstream` or `fscanf` parser.
  * Read the 8-column `.txt` file generated in Phase 1.
  * Store `sigma_src` and `sigma_tgt` alongside the point coordinates.

#### 2. Robust Dynamic Thresholding

**Target File**: `TCF-main/registration/ransac_1pt2pt3pt.cpp` (Function: `ransac1Pt`)

* **Algorithm**: Modify the edge length consistency check.
* **New Logic (with Safety Clamp)**:

  C++
  ```
  // Inside the loop checking edge (i, j) vs (i', j')

  // 1. Safety Clamp: Prevent extreme variance from breaking the logic
  float s_i = std::min(sigma_src[i], 10.0f); 
  float s_j = std::min(sigma_src[j], 10.0f);

  // 2. Compute Margin
  float margin = gamma * (s_i + s_j);

  // 3. Dynamic Threshold
  float dynamic_thresh = std::min(base_threshold + margin, max_threshold);

  // 4. Check
  if (abs(dist_src - dist_tgt) < dynamic_thresh) {
      // Accept edge
  }

  ```
* **Parameters**: Set default `gamma = 1.0` and `max_threshold = 5 * base_threshold`.

**【Action Required】**

Please generate the **Python code for the Uncertainty Head and Loss**, and the **C++ code snippet for the RANSAC modification**.

